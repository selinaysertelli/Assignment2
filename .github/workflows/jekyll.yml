name: Jekyll Site CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Simulate Jekyll CI
        run: |
          echo "This repository uses GitHub Pages"
          echo "Jekyll CI workflow executed successfully"
